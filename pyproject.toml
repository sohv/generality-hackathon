[project]
name = "sandbagging-precision-curve"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
    "openai",
    "matplotlib",
    "inspect-scout>=0.5.0",
]

[tool.setuptools.packages.find]
include = ["src*"]
